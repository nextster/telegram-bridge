name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true
      - name: Verify service and repository-owned integration files
        env:
          SKIP_CODEX_PLUGIN_VALIDATE: "1"
        run: scripts/check.sh
