# syntax=docker/dockerfile:1.7

FROM alpine:3.22

RUN adduser -D -H -u 10001 app
WORKDIR /app
COPY .fly-build/tg-radar /usr/local/bin/tg-radar
RUN mkdir -p /data && chown app:app /data
USER app

ENV PORT=8080
EXPOSE 8080

CMD ["tg-radar", "serve"]
